describe 'Loading Data' do
  # it 'has a load method to return AtBats' do
  #   expect(HitStats).to respond_to(:load)
  # end
  # it 'returns an empty array if theres no file' do
  #   expect(HitStats.load).to eq([])
  # end
  # it 'takes a file to load AtBats from' do
  #   expect(HitStats.load('./spec/at_bat_data.csv')).to be_a(Array)
  # end
  # it 'returns the lines from the CSV as AtBat objects' do
  #   expect(HitStats.load('./spec/at_bat_data.csv').first).to be_a(AtBat)
  # end
  # it 'puts all the data from the csv row into the AtBat model' do
  #
  # end
end
